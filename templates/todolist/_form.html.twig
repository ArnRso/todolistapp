{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="row justify-content-center mt-5">
    <button class="btn btn-success px-5" >{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
